{
  "name": "simple-node-cicd-demo",
  "version": "1.0.0",
  "description": "Minimal Node.js app for demoing a Git -> Jenkins -> Docker -> DockerHub -> Kubernetes pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --colors"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
