apiVersion: v1
kind: Service
metadata:
  name: simple-node-demo-svc
spec:
  type: NodePort
  selector:
    app: simple-node-demo
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
